import google.generativeai as genai
import os

# Конфігурація API-ключа
genai.configure(api_key="AIzaSyBdF9lwYZg71njSjtOJQe5FsB_XWvXcECM")

# Створення моделі
model = genai.GenerativeModel('gemini-2.5-flash')

# Список запитів для тестування
prompts = [
    "Напиши короткий вірш про кіберпанк-майбутнє Львова у стилі Тараса Шевченка.",
    "Є три коробки: в одній тільки яблука, в другій тільки апельсини, а в третій — і яблука, і апельсини. Написи на коробках 'Яблука', 'Апельсини' та 'Суміш' переплутані, тому жоден напис не відповідає дійсності. З якої коробки потрібно витягнути лише один фрукт, щоб визначити, що лежить у кожній коробці, і чому?",
    "Перепиши цей фрагмент коду з Python на JavaScript, зберігши логіку: def factorial(n): return 1 if (n==1 or n==0) else n * factorial(n-1)",
    "Хто був першим програмістом, і яку роль у цьому відіграв Чарльз Беббідж?",
    "Стисло поясни основні принципи теорії відносності Ейнштейна для людини без технічної освіти."
]

print("--- Результати запитів до моделі Gemini ---")
for i, prompt in enumerate(prompts):
    try:
        response = model.generate_content(prompt)
        answer = response.text
    except Exception as e:
        answer = f"Помилка: {e}"

    print(f"\n==================== ЗАПИТ {i + 1} ====================")
    print(f"ЗАПИТ:\n{prompt}")
    print(f"\nВІДПОВІДЬ:\n{answer}")

print("\n==================================================")